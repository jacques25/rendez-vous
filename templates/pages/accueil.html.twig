{% extends "base.html.twig" %}

{% block title %} {% if page %} {{page.title}} {% endif %} {% endblock %}

{% block body %}
 <div class="row-fluid">
 {% if page.filename is defined %}
    <div class="jumbotron" style="background: url({{ vich_uploader_asset(page, 'imageFile')}});" ></div>
 {% endif %}    
 {% if page %}  
     
      <div class="col-sm-12 col-md-12 col-lg-12">
           <h1 class="mt-3">{{page.title }}</h1>
      </div>
        <div class="col-sm-12 col-md-12 col-lg-12">
         <article class="p-4 w-100">{{ page.description|raw }}</article>
   
   </div>
   {% endif %}
   </div>
{% endblock %}


