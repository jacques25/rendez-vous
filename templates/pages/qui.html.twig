{% extends "base.html.twig" %}
{% block body %}
<div class="main-box">
     <div class="content-page"> 
       <img class="images-top mt-4" src="{{ vich_uploader_asset(page, 'imageFile')}}" alt="{{page.filename}}">
           <h1 class="mt-3">{{page.title }}</h1>
         
      
       <div class="mt-4 paragraph">  {{ page.description|raw }}   </div>
     </div>
</div>
      {% endblock %}
