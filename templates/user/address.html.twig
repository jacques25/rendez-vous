	<div class="row">
				  <div class="col-md-4 ">
					{% include 'UserBundle:Default:modulesUsed/utilisateursConnecte.html.twig' %}
				  </div>
				   {% if user.addresses|length != 0 %}
				  <div class="col-md-8 address-user">
                      <div class="card-signin">
						<form action="{{ path('panier_livraison')}}" method="post">
							

								<div class="card-header">
									<h4 class="livraison">
										Adresse de livraison
									</h4>
								</div>
								{% for address in user.addresses  %}
									<div class="card-body">

										<div class="card-text">
											<label class="label-livraison">
												<span>
													{{user.firstname|capitalize }}
													{{user.lastname|capitalize }}
												</span>
												<br/>
												<input type="radio" name="{{address.choix['livraison'] }}" id="optionsRadios1" value="{{ address.id }}" {% if loop.index0 == 0 %} checked="checked" {% endif%}>
												{{ address.street}}
												,
												{{address.cp }}
												{{address.city|upper}}
												<a href="{{path('livraisonadressesuppression', {'id': address.id } ) }}">
													<i class="fa fa-trash"></i>
												</a>
											</label>
										</div>
									</div>
								{% endfor %}
								<div class="card-header">
									<h4 class="livraison">
										Adresse de facturation
									</h4>
								</div>
								{% for address in user.addresses  %}
									<div class="card-body">
										<div class="card-text">

											<label class="label-livraison">
												<span>
													{{user.firstname|capitalize }}
													{{user.lastname|capitalize }}
												</span>
												<br/>
												<input type="radio" name="{{address.choix[facturation]}}" id="optionsRadios1" value="{{ address.id }}" {% if loop.index0 == 0 %} checked="checked" {% endif%}>
												{{ address.street}}
												,
												{{address.cp }}
												{{address.city|upper}}
												<a href="{{path('livraisonadressesuppression', {'id': address.id } ) }}">
													<i class="fa fa-trash"></i>
												</a>
											</label>
										</div>
									</div>
								{% endfor %}
							


							<div class="card-footer">
								<button type="submit" class="btn btn-info btn-lg">
									Valider mes adresses
								</button>
							</div>
                        </form>

					 </div>
					
				  </div>
			       {% endif %}
		   </div>
		<div class="row mt-3 mb-4">
			<div class="col-md-4"></div>
			<div class="col-md-8">


				{{include('user/formAddress.html.twig') }}


			</div>
		</div>
