{% extends  "admin_base.html.twig" %}

		{% block title %}
		Réponse 
		{% endblock %}
   
		{% block body %}
    <div class="container">
			<div class="row contact">
				
					<div class="contact-me modal w-100"  role="dialog" style="display:block" id="modal-response">
					<div class="card-contact">
						<div class="card-header">
							<h2 class="text-center font-italic">
								Réponse  
							</h2>
						</div>
						<div class="card-body-contact">
					
						  <form action="{{ path('admin_contact_response', {'id' : contact.id})}}"  method="POST">
							      <h4 class="mt-3 text-center text-info"> Object du message : {{contact.subject}}</h4>
					             <div class="from-group p-4">   
											  <input  name="response[subject]" type="hidden" value="{{contact.subject}}" class="form-control">
												</div>
												<div class="form-group">
											  <input type="text" name="response[lastname] " placeholder= "{{contact.firstname}}" class="form-control"  value="{{contact.firstname}}">
										   </div> 
											 <div class="form-group">
											 	 <input type="text" name="response[firstname]" placeholder= "{{contact.lastname}}" class="form-control" value="{{contact.lastname}}">
											 </div>
										   <div class="form-group">
											 <input type="email"  name="response[email]" placeholder= "{{contact.email}}" class="form-control" value="{{contact.email}}">
											 </div>
											  <div class="form-group">
												<textarea   rows="8"  class="form-control" name="response[message]" ></textarea>
											 </div>


							<button type="submit" class="btn btn-info btn-block rounded-3 py-2 mt-5">
								Envoyer
							</button>
				</form>
						</div>
					</div>
				</div>
</div>
			</div>
		{% endblock %}

{% macro textarea(name, value, rows = 10, cols = 40) %}
    <textarea name="{{ response.message }}" rows="{{ rows }}" cols="{{ cols }}">{{ value|e }}</textarea>
{% endmacro %}
