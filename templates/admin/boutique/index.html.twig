{% extends 'admin_base.html.twig' %}
{% block title 'Liste des Boutiques'%}
{% block body %}
<div class="container">
<div class="row">
  <h1>Liste des Boutiques</h1>
  <div class="col-12">
    {% for message in app.flashes('success') %}
    <div class="alert alert-success">
      {{ message }}</div>
    {% endfor %}
  </div>
  <table class="table-fluid table-fluid-index">
    <thead>
      <tr>
        <th>Id</th>
        <th>Title</th>
        <th>Image</th>
        <th>Updated_at</th>
        <th>actions</th>
      </tr>
    </thead>
    <tbody>
      {% for boutique in boutiques %}
      <tr>
        <td>
          {{ boutique.id }}</td>
        <td>
          {{ boutique.title }}</td>
     
        <td>
          {% if boutique.filename %}
          <img src="{{ vich_uploader_asset(boutique, 'imageFile')}}" width="100">
          {% endif %}
          </td>
        <td>
          {{ boutique.updatedAt ? boutique.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
        <td>
          <a class="btn btn-info mt-1" href="{{ path('admin_boutique_edit', {'id': boutique.id}) }}"><i class="fa fa-edit"></i></a>
          {{include ('admin/boutique/_delete_form.html.twig')}}
        </td>
      </tr>
      {% else %}
      <tr>
        <td colspan="6">no records found</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</div>
<div class="row">
  <div class="col-md-4">
    <a class="btn btn-info" href="{{ path('admin_boutique_new') }}"><i class="fa fa-plus-circle"></i> Nouveau</a>
  </div>
</div>

</div>
{% endblock %}
