{% extends 'admin_base.html.twig' %}

		{% block title %}
			New Page
		{% endblock %}

		{% block body %}
			<div class="container">
				<h1>
					Create new Page
				</h1>

				{{ include('admin/page/_form.html.twig') }}

				<a href="{{ path('admin_page_index') }}">
					back to list
				</a>

			</div>
		{% endblock %}
