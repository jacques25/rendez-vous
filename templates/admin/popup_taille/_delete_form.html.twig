<form method="post" action="{{ path('admin_popup_taille_delete', {'id': popup_taille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ popup_taille.id) }}">
    <button class="btn btn-danger mt-2"><i class="fa fa-trash"></i></button>
</form>
