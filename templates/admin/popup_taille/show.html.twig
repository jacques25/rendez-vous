{% extends 'admin_base.html.twig' %}

{% block title %}PopupTaille{% endblock %}

{% block body %}
    <h1>PopupTaille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ popup_taille.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ popup_taille.title }}</td>
            </tr>
            <tr>
                <th>Popup</th>
                <td>{{ popup_taille.popup }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_popup_taille_index') }}">back to list</a>

    <a href="{{ path('admin_popup_taille_edit', {'id': popup_taille.id}) }}">edit</a>

    {{ include('admin_popup_taille/_delete_form.html.twig') }}
{% endblock %}
