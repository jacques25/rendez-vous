{{ form_start(form) }}
<div class="row">
 <div class="col-sm-6">
    {{ form_row(form.title) }}

   </div>
   <div class="col-sm-6">
      {{ form_row(form.boutiques, {'id':'multiple'} ) }}
   </div>
</div>
<div class="row">
  <div class="col-sm-6">
     {{form_row(form.descriptionProduits, {'id': 'category'})}}

</div>
  <div class="col-sm-6">
    {{ form_row(form.imageFile)}}
  </div>
  </div>
 <div class="row">
      <div class="col-md-3 mt-3  mx-3">
       <a href="{{ path('admin_produit_index') }}" class="btn btn-primary">
              <i class="fas fa-chevron-left"></i>
              Retour vers la liste
            </a>
    </div>
        <div class="col-md-3 mt-3 mx-3">
          <button class="btn btn-success mt-2">
    {{ button_label|default('Enregistrer') }}
  </button>
    </div>
  
</div>
</div>

{{ form_end(form) }}




