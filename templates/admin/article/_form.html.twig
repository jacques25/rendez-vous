{{ form_start(form) }}

  <div class="col-sm-6">
              {{ form_row(form.title) }}
       </div>
       <div class="col-sm-6">
         {{ form_row(form.category) }}</div>
     </div>


      <div class="col-sm-6">
        {{ form_row(form.description)}}
      </div>
     
      <div class="col-sm-6">
          {{ form_row(form.imageFile)}}
      </div>

<div class="row">
  <a class="btn btn-primary mx-2" href="{{ path('admin.article.index') }}"><i class="fa fa-chevron-left"></i> Retour vers le  liste</a>
{{ form_rest(form) }}
<button class="btn btn-success mb-0">
  {{ button_label|default('Entrgistrer') }}</button>
{{ form_end(form) }}
</div>
