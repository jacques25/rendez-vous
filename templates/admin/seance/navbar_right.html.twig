<ul class="navbar-nav accordion">
			<li class="nav-item">
				{{form_row(form.imageFile)}}
			</li>
			<li class="nav-item">
				<div class="mb-3">
					<h6>
						Choisir une boutique
					</h6>
					{{form_widget(form.category, {'id' : 'category'})}}
				</div>
			</li>
			<li class="nav-item col-12" id="seance_option" data-prototype="{{form_widget(form.seanceOptions.vars.prototype)|e('html_attr')}}">
				<div class="row flex-row">
					{% for row in form.seanceOptions %}

						<div class="card m-2">
							<div class="card-header bg-warning">
								<a href="#item" data-toggle="collapse">
									options des séances
								</a>
							</div>
							<div class="card-body collapse" id="item">
								{{form_widget((row))}}
							</div>
						</div>
					{% endfor %}
				</div>
			</li>
</ul>
