{% extends 'base.html.twig' %}

{% block title %}DescriptionProduit{% endblock %}

{% block body %}
    <h1>DescriptionProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ description_produit.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ description_produit.title }}</td>
            </tr>
            <tr>
                <th>Introduction</th>
                <td>{{ description_produit.introduction }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('description_produit_index') }}">back to list</a>

    <a href="{{ path('description_produit_edit', {'id': description_produit.id}) }}">edit</a>

    {{ include('description_produit/_delete_form.html.twig') }}
{% endblock %}
