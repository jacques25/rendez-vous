{% apply inky_to_html|inline_css(source('@styles/foundation-email.css'), source('@styles/email.css') ) %}
		<container>
			<row class="header">
				<columns>
					<a href="{{ url('app_homepage')}}">
						<img src="{{email.image('@img/rdv.png')}}" class="logo" alt="logo_site">
					</a>
					<h2>	Rendez-vous avec soi</h2>
				</columns>
			</row>

			{% block content %}
			{% endblock %}

			<row class="footer">
				<columns>
				   
       <spacer size="20"></spacer>
        <a class="btn-login" href="{{absolute_url(path('account_login'))}}">Connection</a>
  
				</columns>
			</row>
			<row class="bottom">
				<columns>
					<center>
						<spacer size="20"></spacer>
						<div>
			        Envoyé par {{contact-email}}
              </div>
					</center>
				</columns>
			</row>
		</container>


		{% endapply %}
